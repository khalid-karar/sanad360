# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test output — sample PDFs for manual visual inspection (gitignored, not committed)
test-output/

# Environment files — secrets NEVER live in the repo (see DEPLOYMENT.md).
# Only the .env.example templates are tracked.
.env
.env.*
!.env.example
services/pdf/.env
services/pdf/.env.*
!services/pdf/.env.example

# Supabase CLI local state
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
