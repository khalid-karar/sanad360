# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test output — sample PDFs for manual visual inspection (gitignored, not committed)
test-output/

# Playwright E2E run artifacts (screenshots/videos/traces, HTML report) — CI
# uploads these as workflow artifacts on failure instead of committing them.
test-results/
playwright-report/

# E2E email-capture hook output (services/pdf/src/lib/email.ts) — only ever
# written when E2E_CAPTURE_EMAIL=1, never in production; local default path.
.e2e-captured-emails.jsonl

# Environment files — secrets NEVER live in the repo (see DEPLOYMENT.md).
# Only the .env.example templates are tracked.
.env
.env.*
!.env.example
services/pdf/.env
services/pdf/.env.*
!services/pdf/.env.example

# Supabase CLI local state
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
